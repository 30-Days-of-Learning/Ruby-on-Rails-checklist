<h1>To-dos</h1>
<%= link_to "Add new todo", new_todo_path %>

<table>
  <tr>
    <th>Title</th>
    <th>Status</th>
  </tr>
  <% @todos.each do |todo| %>
    <tr>
      <td><%= link_to todo.title, edit_todo_path(todo) %></td>
      <td><%= todo.completed %></td>
    </tr>
  <% end %>
</table>
